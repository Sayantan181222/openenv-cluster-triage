# Core web framework (serves both the OpenEnv API and Gradio UI)
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Gradio UI removed
requests>=2.31.0

# Pydantic (models, validation)
pydantic==2.10.4

# OpenAI client (for inference.py LLM calls)
openai==1.58.1
openenv-core<=0.2.0

# Environment variable loading
python-dotenv==1.0.1

# HTTP support for health checks in Docker
httpx==0.28.1
